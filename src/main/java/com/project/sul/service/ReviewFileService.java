package com.project.sul.service;

import java.io.FileOutputStream;
import java.util.UUID;

public class ReviewFileService {

//    public String rvUploadFile(String rvUploadPath, String rvOriFileName, byte[] fileData) throws Exception{
//        UUID uuid = UUID.randomUUID();
//        String extension = rvOriFileName.substring(rvOriFileName.lastIndexOf("."));
//        String savedFileName = uuid.toString() + extension;
//        String fileUploadFullUrl = rvUploadPath + "/" + savedFileName;
//        FileOutputStream fos = new FileOutputStream(fileUploadFullUrl);
//        fos.write(fileData);
//        fos.close();
//        return savedFileName;
//    }
}
